package me.rexysaur.void_.Client.util;

public enum Colours {
	WHITE(255, 255, 255),
	BLACK(0, 0, 0);
	
	Colours(int... RGB)
	{
		this.red = RGB[0];
		this.green = RGB[1];
		this.blue = RGB[2];
	}

}
