package me.rexysaur.void_.Client.util;

import java.util.Timer;
import java.util.TimerTask;
import java.util.concurrent.Callable;

public abstract class Interval {
	
	public Interval(int time, Callable<Integer> func)
	{
		Timer timer = new Timer();
		timer.schedule(new IntervalTimer(time, func), null);
	}

	class IntervalTimer extends TimerTask{
		public Callable<Integer> func;
		
		public IntervalTimer(Callable<Integer> func) {
			this.func = func;
		}

		@Override
		public void run() {
			try {
				this.func.call();
			} catch (Exception e) {
				e.printStackTrace();
			}
			
			
		}
	}
}